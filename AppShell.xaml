<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="firstproject.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:firstproject.Views"
    FlyoutBehavior="Locked"
    Title="firstproject">

    <ShellContent
        Title="1"
        ContentTemplate="{DataTemplate local:firstPage}"
        Route="firstPage" />

    <ShellContent
    Title="2"
    ContentTemplate="{DataTemplate local:SecondPage}"
    Route="SecondPage" />

    <ShellContent
    Title="Age "
    ContentTemplate="{DataTemplate local:AgePage}"
    Route="AgePage" />

     <ShellContent
    Title="Age Candels"
    ContentTemplate="{DataTemplate local:AgeCandelsPage}"
    Route="AgeCandelsPage" />

        <ShellContent
    Title="Slider and Candels"
    ContentTemplate="{DataTemplate local:SliderCandelsPage}"
    Route="SliderCandelsPage" />

</Shell>
